// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#048748', // Replace with your primary color
    },
    secondary: {
      main: '#000000', // Replace with your secondary color
    },
  },
});

export default theme;